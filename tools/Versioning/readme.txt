Versioning.exe

Versioning opens ./inc/Blib.h
Reads the content.
Replaces #define BLIB_VERSION "1.0.0.0" with #define BLIB_VERSION "v.dd.mm.yyyy"
Write the file back

Auto version keeping, first number is copied and saved, date is inserted for versioning.

That's all folks.